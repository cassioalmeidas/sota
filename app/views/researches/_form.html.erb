<%= simple_form_for(research) do |f| %>
  <%= f.input :description  %>
  <%= link_to t('buttons.cancel'), researches_path, class: 'btn btn-secondary' %>
  <%= f.button :submit, class: 'btn-success' %>
<% end %>
